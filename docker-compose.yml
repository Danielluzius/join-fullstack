services:
  backend:
    build: ./backend
    restart: always
    ports:
      - '127.0.0.1:8001:8000'
    env_file:
      - ./backend/.env
    volumes:
      - sqlite_data:/app/data

volumes:
  sqlite_data:
