services:
  backend:
    build: ./backend
    restart: always
    ports:
      - '8000:8000'
    env_file:
      - ./backend/.env
    volumes:
      - sqlite_data:/app/db.sqlite3

volumes:
  sqlite_data:
